# ─────────────────────────────────────────────────────────────
# S-Core Environment Configuration
# Copy this file to .env and fill in your values
# ─────────────────────────────────────────────────────────────

# Required: Groq API Key for LLM operations
GROQ_API_KEY=your_groq_api_key_here

# MongoDB Configuration (optional — defaults shown)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=score_db

# ChromaDB Storage Path (optional)
CHROMA_PATH=./chroma_data

# JWT Authentication (optional — defaults shown)
JWT_SECRET=default-key-score
JWT_EXPIRY_HOURS=72
